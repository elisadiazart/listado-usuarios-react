import styled from "styled-components";

const StyledDiv = styled.div`
    width: 630px;
    display: flex;
    justify-content: space-between;
    margin: 2rem auto;
`

const StyledCheckbox = styled.div`
    display: flex;
    gap: 1rem;
`

export{StyledDiv, StyledCheckbox}